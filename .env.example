#-----------------------------------------------------------------------
# HolySword Framework - 环境配置文件
#-----------------------------------------------------------------------
#
# 该文件定义应用的环境变量配置。
# 复制此文件为 .env 并根据你的环境修改配置值。
#
# 注意：
# - .env 文件不应提交到版本控制系统
# - 不要在此文件中存储敏感信息（密码、API 密钥等）
# - 使用 env() 辅助函数读取这些配置
#
# 使用方法：
# $debug = env('APP_DEBUG', false);
# $appName = env('APP_NAME', 'MyApp');
#-----------------------------------------------------------------------

#-----------------------------------------------------------------------
# 应用基本配置
#-----------------------------------------------------------------------

# 应用名称 - 用于显示和标识
APP_NAME=HolySword

# 运行环境 - 可选值: local(本地), production(生产), testing(测试)
# local: 开发环境，显示详细错误信息
# production: 生产环境，隐藏敏感错误信息
# testing: 测试环境
APP_ENV=local

# 调试模式 - true/false
# true: 显示详细的错误信息（包含堆栈跟踪）
# false: 只显示简单的错误消息
APP_DEBUG=true

# 应用 URL - 应用的完整 URL（用于生成链接等）
APP_URL=http://localhost

# 应用密钥 - 用于加密和安全操作
# 请生成一个随机的 32 字符字符串
APP_KEY=

#-----------------------------------------------------------------------
# 地区与语言配置
#-----------------------------------------------------------------------

# 时区设置 - 参考: https://www.php.net/manual/zh/timezones.php
APP_TIMEZONE=Asia/Shanghai

# 默认语言 - 应用的默认显示语言
APP_LOCALE=zh_CN

#-----------------------------------------------------------------------
# 数据库配置
#-----------------------------------------------------------------------

# 默认数据库连接 - mysql, pgsql, sqlite, sqlsrv
DB_CONNECTION=mysql

#-----------------------------------------------------------------------
# MySQL 主库配置
#-----------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=holysword
DB_USERNAME=root
DB_PASSWORD=
DB_PREFIX=
DB_CHARSET=utf8mb4

#-----------------------------------------------------------------------
# MySQL 从库配置（可选，用于读写分离）
#-----------------------------------------------------------------------
# DB_READ_HOST=localhost
# DB_READ_PORT=3306
# DB_READ_DATABASE=holysword
# DB_READ_USERNAME=root
# DB_READ_PASSWORD=

#-----------------------------------------------------------------------
# PostgreSQL 配置（可选）
#-----------------------------------------------------------------------
# PGSQL_HOST=localhost
# PGSQL_PORT=5432
# PGSQL_DATABASE=holysword
# PGSQL_USERNAME=postgres
# PGSQL_PASSWORD=
# PGSQL_PREFIX=

#-----------------------------------------------------------------------
# SQLite 配置（可选）
#-----------------------------------------------------------------------
# SQLITE_DATABASE=storage/database/database.sqlite

#-----------------------------------------------------------------------
# SQL Server 配置（可选）
#-----------------------------------------------------------------------
# SQLSRV_HOST=localhost
# SQLSRV_PORT=1433
# SQLSRV_DATABASE=holysword
# SQLSRV_USERNAME=sa
# SQLSRV_PASSWORD=
